export { 
  ReportData, 
  PdfOptions, 
  ReportOptions, 
  SectionParams,
  AIConditionResult,
  AdjustmentBreakdown 
} from '@/types/valuation';

export interface PdfMetadata {
  title: string;
  author: string;
  subject: string;
  creator: string;
}
