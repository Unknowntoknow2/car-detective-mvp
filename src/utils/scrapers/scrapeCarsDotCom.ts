
// Temporarily disabled for MVP - missing cheerio dependency
export async function scrapeCarsDotCom(query: string, zip: string) {
  // Disabled for MVP - missing cheerio dependency
  console.log('Cars.com scraping disabled for MVP');
  return [];
}
