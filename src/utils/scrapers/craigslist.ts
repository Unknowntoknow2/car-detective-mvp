
// Temporarily disabled for MVP - missing puppeteer-extra and cheerio dependencies
export async function fetchCraigslistListings(
  make: string,
  model: string,
  zip = "95814",
  maxResults = 10,
) {
  // Disabled for MVP - missing dependencies
  console.log('Craigslist scraping disabled for MVP');
  return [];
}
