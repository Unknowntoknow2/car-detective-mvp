{
  "$schema": "https://deno.land/x/deno/schemas/deno.jsonc",
  "importMap": "./import_map.json",
  "lint": { "rules": { "exclude": [] } },
  "fmt": { "lineWidth": 100, "indentWidth": 2 },
  "tasks": {
    "test": "deno test --allow-env --allow-net --coverage=coverage/deno",
    "lint": "deno lint",
    "fmt": "deno fmt"
  }
}
